# Contributing

- Use clear, readable code. Follow existing naming and formatting.
- Prefer PyTorch for new features; keep TensorFlow parity tests passing when feasible.
- Add unit tests for fixes and features. Keep tests deterministic where possible.
- Avoid print statements in libraries; return values or use logging where needed.
- Document changes in CHANGELOG.md.
- Keep public APIs stable; mark experimental modules clearly.
